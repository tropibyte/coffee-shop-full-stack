# ---------------------------------------------------------------------------
# Development and CI dependencies.  Install on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
# ---------------------------------------------------------------------------
-r requirements.txt

# --- Test runner -----------------------------------------------------------
pytest==9.1.1
pytest-cov==7.1.0
coverage==7.16.1

# --- Test doubles ----------------------------------------------------------
responses==0.26.3        # records/replays the Auth0 JWKS + Management API
freezegun==1.5.5         # travels through time to test token expiry

# --- Style and static analysis ---------------------------------------------
flake8==7.3.0
pycodestyle==2.14.0
pyflakes==3.4.0
black==26.5.1
isort==9.0.1

# --- Security tooling ------------------------------------------------------
bandit==1.9.4            # source-level security linter
pip-audit==2.7.3         # dependency CVE scanner
