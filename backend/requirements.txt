# ---------------------------------------------------------------------------
# Coffee Shop Full Stack - backend runtime dependencies
#
# Pinned to exact versions so that a reviewer's `pip install -r requirements.txt`
# resolves to the same tree that CI tested.  See docs/DEPENDENCIES.md for the
# rationale behind each choice, including why python-jose was replaced.
# ---------------------------------------------------------------------------

# --- Web framework ---------------------------------------------------------
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.5.0
blinker==1.9.0
MarkupSafe==3.0.3

# --- Persistence -----------------------------------------------------------
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.54
Flask-Migrate==4.1.0
alembic==1.20.0

# --- Cross-origin access for the Ionic frontend ----------------------------
Flask-Cors==6.0.5

# --- Authentication / authorisation ----------------------------------------
# PyJWT + cryptography replace python-jose; see docs/DEPENDENCIES.md.
PyJWT==2.14.0
cryptography==50.0.1

# --- Outbound calls to the Auth0 Management API ----------------------------
requests==2.34.2
urllib3==2.8.0
certifi==2026.7.22

# --- Abuse resistance ------------------------------------------------------
Flask-Limiter==4.1.1
limits==5.8.0

# --- Configuration ---------------------------------------------------------
python-dotenv==1.2.3

# --- Production WSGI server (used by the Docker image and Azure App Service)
gunicorn==26.2.0
